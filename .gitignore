target/
*.d
*.o

# local credentials — never commit
.env
.env.*
!.env.example
.direnv/

# beads-rs local state — issues.jsonl is a symlink to per-user data
.beads/

# upstream C reference drop — read-only reference, not ours to commit
C_CODE/
pcloudcc
.claude/
